% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pca_spatial.R
\name{lift,ClusterReduction,spec_pca-method}
\alias{lift,ClusterReduction,spec_pca-method}
\title{Lift parcel/cluster-local PCA bases for ClusterReduction}
\usage{
\S4method{lift}{ClusterReduction,spec_pca}(
  reduction,
  basis_spec,
  data = NULL,
  center = TRUE,
  scale = FALSE,
  offset = NULL,
  backend = c("auto", "svds", "svd"),
  ...
)
}
\arguments{
\item{reduction}{ClusterReduction describing voxel-to-cluster map.}

\item{basis_spec}{PCA basis specification (from `basis_pca()`).}

\item{data}{Numeric matrix (time x voxels in mask order). Required.}

\item{center}{Logical; center voxels before PCA (default TRUE).}

\item{scale}{Logical; scale voxels before PCA (default FALSE).}

\item{offset}{Optional numeric vector of voxel means (length n_vox). If provided
and `center = TRUE`, this is used instead of recomputing `colMeans(data)`.}

\item{backend}{SVD backend: "auto" (default), "svds" (RSpectra), or "svd" (base).}

\item{...}{Unused.}
}
\value{
A sparse Matrix (voxels x components) with attribute
  `fmrilatent.singular_values` giving per-component singular values.
}
\description{
Computes PCA eigenvectors within each cluster and assembles a global
block-sparse loadings matrix (voxels x components). This is typically used
with `encode(..., spec_space_pca(...), reduction = ...)`.
}
