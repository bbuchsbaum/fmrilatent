% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slepian_temporal.R
\name{dpss_time_basis}
\alias{dpss_time_basis}
\title{DPSS temporal basis (Slepian sequences)}
\usage{
dpss_time_basis(
  n_time,
  tr,
  bandwidth,
  k = NULL,
  backend = c("tridiag", "dense")
)
}
\arguments{
\item{n_time}{Integer length of the time dimension.}

\item{tr}{Repetition time in seconds.}

\item{bandwidth}{Half-bandwidth in Hz (typical BOLD range 0.008–0.1).}

\item{k}{Optional number of tapers; defaults to \code{floor(2 * NW) - 1}
where \code{NW = n_time * bandwidth * tr}. Clamped to \code{[1, n_time]}.}

\item{backend}{Either \code{"tridiag"} (default, O(n^2)) or \code{"dense"} (O(n^3), debugging).}
}
\value{
Numeric matrix (n_time x k) with orthonormal columns.
}
\description{
Generates Discrete Prolate Spheroidal Sequences (DPSS) for a given series
length and bandwidth. Uses an internal RcppEigen solver over the prolate
matrix; suitable for moderate `n_time`. For very long series, a tridiagonal
solver can replace the backend with the same interface.
}
