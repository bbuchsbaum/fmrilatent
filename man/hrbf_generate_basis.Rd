% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hrbf.R
\name{hrbf_generate_basis}
\alias{hrbf_generate_basis}
\alias{hrbf_project_matrix}
\alias{hrbf_reconstruct_matrix}
\title{Hierarchical radial basis functions (HRBF) for latent fMRI}
\usage{
hrbf_generate_basis(params, mask)

hrbf_project_matrix(X, mask, params)

hrbf_reconstruct_matrix(coeff, mask, params)
}
\arguments{
\item{params}{List with fields:
- `sigma0` (numeric, default 6)
- `levels` (integer, default 3)
- `radius_factor` (numeric, default 2.5)
- `num_extra_fine_levels` (integer, default 0)
- `kernel_type` (\"gaussian\" or \"wendland_c6\", alias \"wendland_c4\")
- `seed` (integer) for deterministic Poisson sampling}

\item{mask}{`LogicalNeuroVol` mask defining voxel locations.}

\item{X}{Numeric matrix with time in rows and voxels in columns.}

\item{coeff}{Coefficient matrix with rows = time points.}
}
\value{
For `hrbf_generate_basis`, a sparse matrix with one row per
  HRBF atom and columns matching mask voxels.
}
\description{
Utilities to generate analytic HRBF bases, project data, reconstruct,
and build `LatentNeuroVec` objects. Parameters are kept simple and
in-R only (no descriptors or HDF5).
}
