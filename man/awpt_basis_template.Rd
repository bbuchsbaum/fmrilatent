% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/awpt.R
\name{awpt_basis_template}
\alias{awpt_basis_template}
\title{Build an AWPT basis template}
\usage{
awpt_basis_template(
  parcellation,
  basis_spec = basis_awpt_wavelet(),
  loadings = NULL,
  anatomical_operator = NULL,
  conductance = NULL,
  coefficient_roughness = NULL,
  center = FALSE,
  ridge = 1e-08,
  label = "awpt_wavelet",
  ...
)
}
\arguments{
\item{parcellation}{A \code{ClusterReduction} or \code{ClusteredNeuroVol}.}

\item{basis_spec}{An AWPT basis specification created by \code{basis_awpt_wavelet()}.}

\item{loadings}{Optional explicit template loadings matrix. When supplied,
fmrilatent skips wave-packet lifting and uses these loadings directly as
the decoder basis \eqn{B}.}

\item{anatomical_operator}{Optional field-space roughness operator on the
template domain. When supplied, the coefficient roughness is computed as
\eqn{Q = B^T L B}. In the current v1 implementation this affects the
roughness penalty, not the basis construction itself.}

\item{conductance}{Optional symmetric field-space conductance matrix. When
supplied, fmrilatent builds the corresponding graph Laplacian and then
forms \eqn{Q = B^T L B}. As with \code{anatomical_operator}, this shapes
the v1 roughness model rather than directly adapting the lifted basis.}

\item{coefficient_roughness}{Optional coefficient-space roughness matrix.
This bypasses field-space construction and is stored directly as \eqn{Q}.}

\item{center}{Logical; if \code{TRUE}, center data before projection.}

\item{ridge}{Small positive ridge added to the Gram diagonal if needed.}

\item{label}{Optional label stored in metadata.}

\item{...}{Additional arguments passed to the underlying lift path.}
}
\value{
An \code{AWPTBasisTemplate}.
}
\description{
Build an AWPT basis template
}
