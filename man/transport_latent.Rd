% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transport_latent.R
\name{transport_latent}
\alias{transport_latent}
\title{Construct a transport-backed implicit latent object}
\usage{
transport_latent(
  coeff_raw,
  basis_asset,
  field_operator = NULL,
  observation_operator = NULL,
  mask = NULL,
  domain = NULL,
  support = NULL,
  coeff_analysis = NULL,
  analysis_transform = NULL,
  offset = numeric(0),
  run_info = NULL,
  meta = list()
)
}
\arguments{
\item{coeff_raw}{Raw coefficient matrix (time x k).}

\item{basis_asset}{Shared basis asset.}

\item{field_operator}{Subject field operator satisfying the
contract documented in \code{\link{encode_operator}()}.}

\item{observation_operator}{Legacy alias for \code{field_operator}.}

\item{mask}{Target-domain mask for volumetric reconstruction.}

\item{domain}{Optional target domain for non-volumetric reconstruction.}

\item{support}{Optional target support for non-volumetric reconstruction.}

\item{coeff_analysis}{Optional analysis-coordinate coefficient matrix (time x k).}

\item{analysis_transform}{Optional transform descriptor from raw to analysis coordinates.}

\item{offset}{Optional voxel offset vector in target space.}

\item{run_info}{Optional run metadata.}

\item{meta}{Optional metadata list.}
}
\value{
A \code{TransportLatent} object inheriting from \code{ImplicitLatent}.
}
\description{
Construct a transport-backed implicit latent object
}
\details{
`transport_latent()` stores the `fmrilatent` side of the operator-backed
workflow: shared basis asset, subject field operator, raw and
analysis-space coefficients, and decoder views for native or template
projection. It does not fit GLMs or carry statistical summaries. Downstream
model-fitting code should ordinarily consume
\code{coef_time(x, coordinates = "analysis")}, then return coefficient-space
effects and covariance matrices for projection with
\code{decode_coefficients()} and \code{decode_covariance()}.
}
