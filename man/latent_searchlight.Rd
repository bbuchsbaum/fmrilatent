% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/searchlight_utils.R
\name{latent_searchlight}
\alias{latent_searchlight}
\title{Apply a user-defined function in latent space over neighborhoods}
\usage{
latent_searchlight(basis, loadings, neighborhoods, fun, ...)
}
\arguments{
\item{basis}{Matrix or BasisHandle (time x k) from a LatentNeuroVec.}

\item{loadings}{Matrix or LoadingsHandle (voxels x k) from a LatentNeuroVec.}

\item{neighborhoods}{List of integer vectors of voxel indices (mask order).}

\item{fun}{Function(B, L_V, M_V, idx, ...) returning a result per neighborhood.}

\item{...}{Passed through to `fun`.}
}
\value{
List of results, one per neighborhood.
}
\description{
Runs a user-supplied function `fun` for each neighborhood using only latent
quantities. `fun` is called with arguments `(B, L_V, M_V, idx, ...)`, where:
  - `B` is the basis matrix (time x k)
  - `L_V` is the loadings restricted to the neighborhood (|V| x k)
  - `M_V = t(L_V) %*% L_V` (k x k)
  - `idx` is the voxel indices of the neighborhood
`fun` should return any R object; results are collected in a list.
}
