% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boldzip_sr.R
\name{boldzip_sr_encode}
\alias{boldzip_sr_encode}
\title{Encode a matrix with experimental BOLDZip-SR compression}
\usage{
boldzip_sr_encode(
  X,
  spatial_basis = NULL,
  k_carriers = 96L,
  temporal_k = NULL,
  q_texture = 2L,
  texture_lags = 0L,
  reliability = boldzip_reliability(),
  quantization = boldzip_quantization(),
  events = boldzip_events(),
  center = TRUE,
  label = NULL
)
}
\arguments{
\item{X}{Numeric matrix with dimensions `voxels x time`.}

\item{spatial_basis}{Optional `BoldZipSRSpatialBasis` object or list with
`phi_c` and `phi_d` matrices. If omitted, the detail basis is identity and
carriers are learned from `X` directly.}

\item{k_carriers}{Number of carrier time courses to learn.}

\item{temporal_k}{Number of DCT coefficients per carrier. Defaults to
`ceiling(n_time / 4)`.}

\item{q_texture}{Maximum number of carrier loadings per detail atom.}

\item{texture_lags}{Integer vector of allowed carrier lags for texture
loading fits. A positive lag uses `Z_k(t - lag)`.}

\item{reliability}{Reliability settings from [boldzip_reliability()].}

\item{quantization}{Quantization settings from [boldzip_quantization()].}

\item{events}{Event settings from [boldzip_events()].}

\item{center}{Whether to store and remove a voxel-wise mean before fitting.}

\item{label}{Optional label stored in metadata.}
}
\value{
A `BoldZipSR` object.
}
\description{
`boldzip_sr_encode()` is an experimental matrix-level implementation of
Split-Reliable Graph Carrier Compression. It expects a matrix with rows as
voxels or grayordinates and columns as time points. The implementation stores
temporally compressed carriers, sparse high-resolution texture loadings, and
sparse split-reliable residual events. It is intended as a research prototype,
not a production binary codec.
}
