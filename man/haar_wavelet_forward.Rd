% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/haar_wavelet.R
\name{haar_wavelet_forward}
\alias{haar_wavelet_forward}
\title{Forward Haar wavelet transform (mask-adaptive, Morton order)}
\usage{
haar_wavelet_forward(
  X,
  mask,
  levels = NULL,
  z_seed = 42L,
  threshold = list(type = "none", value = 0)
)
}
\arguments{
\item{X}{Numeric matrix with time in rows and voxels (masked) in columns.}

\item{mask}{3D logical array or LogicalNeuroVol defining the mask.}

\item{levels}{Number of decomposition levels; defaults to ceil(log2(max(dim(mask)))).}

\item{z_seed}{Integer seed for Morton ordering (tie-breaking).}

\item{threshold}{List with fields `type` ("none" | "absolute" | "relative_to_root_std") and `value`.}
}
\value{
List with elements `coeff` (list(root, detail)), `meta` (counts, Morton hash, etc.).
}
\description{
Forward Haar wavelet transform (mask-adaptive, Morton order)
}
