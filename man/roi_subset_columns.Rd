% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/implicit_latent.R
\name{roi_subset_columns}
\alias{roi_subset_columns}
\title{Subset reconstruction matrix columns by ROI mask}
\usage{
roi_subset_columns(rec_mat, mask_arr, roi_mask = NULL)
}
\arguments{
\item{rec_mat}{Numeric matrix (time x voxels-in-mask) to subset.}

\item{mask_arr}{Logical array (the full brain mask).}

\item{roi_mask}{Logical array (the ROI mask), or \code{NULL} to return
\code{rec_mat} unchanged.}
}
\value{
The subsetted matrix, or \code{rec_mat} unchanged when
  \code{roi_mask} is \code{NULL}.
}
\description{
Extracts columns from a reconstruction matrix that correspond to voxels
inside an ROI mask. This is a shared utility used by decoder functions
to handle ROI subsetting consistently.
}
\examples{
mask <- array(c(TRUE, TRUE, FALSE, TRUE), dim = c(2, 2, 1))
rec  <- matrix(1:9, nrow = 3, ncol = 3)  # 3 time x 3 masked voxels
roi  <- array(c(TRUE, FALSE, FALSE, TRUE), dim = c(2, 2, 1))
roi_subset_columns(rec, mask, roi)  # keeps columns 1 and 3
}
