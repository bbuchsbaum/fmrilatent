% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/encode.R
\name{encode_operator}
\alias{encode_operator}
\title{Encode data using a shared basis asset and subject field operator}
\usage{
encode_operator(
  x,
  template,
  field_operator = NULL,
  observation_operator = NULL,
  mask = NULL,
  domain = NULL,
  support = NULL,
  lambda = 0,
  center = TRUE,
  run_info = NULL,
  spatial_lambda = lambda,
  spatial_penalty = NULL,
  temporal_lambda = 0,
  temporal_order = 1L,
  sparse_lambda = 0,
  sparse_mode = c("none", "group_l2", "lasso"),
  max_iter = 200L,
  tol = 1e-06,
  label = "",
  ...
)
}
\arguments{
\item{x}{Numeric matrix (time x target samples) or a \code{NeuroVec}.}

\item{template}{Shared basis asset providing \code{basis_decoder()}.}

\item{field_operator}{Subject field operator mapping template field
samples to observed native samples.}

\item{observation_operator}{Legacy alias for \code{field_operator}.}

\item{mask}{Optional volumetric target mask for the field-operator
target domain.}

\item{domain}{Optional non-volumetric target domain, for example a surface
geometry.}

\item{support}{Optional target support aligned to \code{domain}, for example
vertex indices on a surface.
At least one of \code{mask} or \code{support} must be available either
explicitly or via \code{field_operator$provenance}.}

\item{lambda}{Ridge penalty strength.}

\item{center}{Logical; if \code{TRUE}, center target samples before solving.}

\item{run_info}{Optional run metadata carried on the resulting latent object.}

\item{spatial_lambda}{Strength of the spatial coefficient penalty.}

\item{spatial_penalty}{Optional coefficient-space roughness matrix or diagonal weights.}

\item{temporal_lambda}{Strength of temporal smoothing.}

\item{temporal_order}{Difference order used for temporal smoothing.}

\item{sparse_lambda}{Strength of optional sparse coefficient shrinkage.}

\item{sparse_mode}{Sparse penalty mode. Use \code{"group_l2"} for atom-wise group shrinkage.}

\item{max_iter}{Maximum iterations for sparse AWPT optimization.}

\item{tol}{Relative convergence tolerance for sparse AWPT optimization.}

\item{label}{Optional label stored in metadata.}

\item{...}{Reserved for future extensions.}
}
\value{
A \code{TransportLatent} object.
}
\description{
Encode data using a shared basis asset and subject field operator
}
\details{
The external field-operator contract is intentionally narrow. `fmrilatent`
consumes an operator-like object with:
\describe{
  \item{\code{n_source}, \code{n_target}}{Source and target dimensions.}
  \item{\code{source_domain_id}, \code{target_domain_id}}{Stable domain identifiers.}
  \item{\code{forward(z)}}{Applies the operator to template field samples.}
  \item{\code{adjoint_apply(y)}}{Applies the adjoint map.}
  \item{\code{provenance$target_mask}}{Optional target-domain mask when
  the caller does not pass \code{mask} explicitly.}
}

On the main quadratic and sparse transport/AWPT paths, coefficient recovery
is matrix-free: `fmrilatent` uses the operator's forward and adjoint methods
rather than materializing the full subject decoder.
}
