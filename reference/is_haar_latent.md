# Test if object is a Haar latent representation

Test if object is a Haar latent representation

## Usage

``` r
is_haar_latent(x)
```

## Arguments

- x:

  Object to test

## Value

Logical indicating if x is a Haar latent
