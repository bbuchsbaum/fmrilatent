# Spatial HRBF spec

Spatial HRBF spec

## Usage

``` r
spec_space_hrbf(params = list())
```

## Arguments

- params:

  HRBF parameter list (sigma0, levels, radius_factor, kernel_type,
  seed).

## Value

A \`spec_space_hrbf\` object.
