# Temporal DCT spec

Temporal DCT spec

## Usage

``` r
spec_time_dct(k, norm = c("ortho", "none"))
```

## Arguments

- k:

  Components.

- norm:

  Normalization ("ortho" or "none").

## Value

A \`spec_time_dct\` object.
