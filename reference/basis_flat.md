# Create a flat basis specification

\`basis_flat()\` creates a descriptor for methods that should lift a
reduction without estimating local components, typically as a simple
parcel/cluster indicator basis.

## Usage

``` r
basis_flat()
```

## Value

An empty list with class \`spec_flat\`.
