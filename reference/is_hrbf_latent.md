# Check if latent object carries HRBF metadata

Check if latent object carries HRBF metadata

## Usage

``` r
is_hrbf_latent(x)
```

## Arguments

- x:

  Object to test

## Value

Logical indicating if x has HRBF metadata
